<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by mobile_AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="mobile_AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="mobile_AppTheme" parent="mobile_AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
	<style name="mobile_CustomTextApperance" >
	    <item name="android:textColor">#fff</item>  
	    <item name="android:textSize">18sp</item>  
	    <item name="android:textStyle">bold</item>  
	</style>


	<!-- Application theme. -->
	<style name="mobile_CustomAppTheme" parent="android:Theme.Light">
	    <item name="android:windowTitleSize">40dp</item>
	    <item name="android:windowIsTranslucent">true</item>
	</style>  
	
	<style name="mobile_pop_add_ainm" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>
    <style name="mobile_myProgressBar" parent="android:Widget.DeviceDefault.Light.ProgressBar.Horizontal">
    </style>
    
    <style name="mobile_succinct_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>

    <style name="mobile_succinctProgressDialog" parent="@style/mobile_succinct_dialog">
        <item name="android:windowBackground">@drawable/shape_succinct_dialog</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <!--自定义dialog背景全透明无边框theme -->
    <style name="mobile_MyDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>